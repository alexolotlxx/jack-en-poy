PSEUDOCODE

Doesnt include pseudocode 

// 1. getComputerChoice function
GET computer choice

// 2. getHumanChoice
GET human choice

// 3. playRound function
CALL getComputerChoice
    RETURNING computerChoice
CALL getHumanChoice
    RETURNING humanChoice
IF human chooses rock and computer chooses scissors THEN
    PRINT human wins
    INCREMENT humanScore
ELSE IF human chooses rock and computer chooses paper THEN
    PRINT human loses
ELSE IF human chooses paper and computer chooses rock THEN
    PRINT human wins
    INCREMENT humanScore
ELSE IF human chooses paper and computer chooses scissors THEN
    PRINT human loses
ELSE IF human chooses scissors and computer chooses paper THEN
    PRINT human wins
    INCREMENT humanScore
ELSE IF human chooses scissors and computer chooses rock THEN
    PRINT human loses
ELSE
    PRINT it's a tie
ENDIF


//4. playGame function
INIT count variable to 1, totalRounds to 5, humanScore and computerScore to 0
WHILE count variable is less than or equal to 5
    CALL playRound function
ENDWHILE

IF humanScore > computerScore THEN
    PRINT human wins the game
ELSE
    PRINT computer wins the game
ENDIF